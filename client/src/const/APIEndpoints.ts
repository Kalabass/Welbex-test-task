const AUTH_ENDPOINTS = {
  LOGIN: '/auth/login',
  REG: '/auth/reg',
  REFRESH: '/auth/refresh',
};

const POST_ENDPOINTS = {
  BASE: 'posts',
};

export const API_ENDPOINTS = {
  AUTH: AUTH_ENDPOINTS,
  POST: POST_ENDPOINTS,
};
